import styled from 'styled-components';

export const Box = styled.header `
    padding-top: 30px;
    height: 42px;
    display: flex;
    flex-direction: row;
    justify-content: space-between;

`
